# 2026-05-21 — Development Log

## 16-Item Improvement Plan (S-1~S-5, U-1~U-6, M-1~M-6)

- **Files**: app.py, download_service.py, i18n.py, static/js/script.js, static/css/styles.css, templates/index.html, locales/*.json (16 files), test_app.py, test_i18n.py, .github/workflows/ci.yml
- **Purpose**: Implement all 16 improvement items from the multi-agent design discussion: security hardening, UX enhancements, and experience improvements
- **Decisions**:
  - GIL ordering: write `_completed[job_id]` before `_jobs["status"] = "done"` to prevent race condition
  - Cancel uses `threading.Event` with `.get()` (not pop) so thread's `finally` block cleans up `_cancel_events`
  - Progress tracking uses `downloaded_bytes / total_bytes` (not deprecated `_percent_str`)
  - One-time file-serve link: `_completed` popped on first successful serve
- **Issues**:
  - Task 6: Missing `job_id in _jobs` guard in progress hook — fixed (KeyError risk on cancellation)
  - Task 12: `assert get_lang() == 'en'` failed on Korean OS — correctly adapted to `in i18n.SUPPORTED`
  - Final: `_jobs` cancelled state leaked (only done/error were popped) — fixed to include "cancelled"
  - Final: `ui.queue_empty` key defined but not rendered — wired `<p>` in HTML + CSS + JS hide logic

## Project Constitution Compliance

- **Files**: scripts/audit.sh, scripts/audit.ps1, scripts/dev-sync.sh, scripts/dev-sync.ps1, .claude/settings.json, .claude/commands/sync.md, .claude/commands/memlog.md, .claude/commands/new-task.md, agents/pm.md, docs/context.md, memory/MEMORY.md
- **Purpose**: Align project structure with C:/git/CLAUDE.md Project Constitution
- **Decisions**:
  - Skipped `src/` migration (low priority, high risk for running project)
  - `agents/pm.md` created at standard location; `docs/agent-config/agents.md` kept for backward compat
  - `docs/context.md` created as SSoT; `docs/agent-config/shared.md` kept for existing references
- **Issues**: None
## 2026-05-21 (Session 2) ??ffmpeg Auto-install & Desktop App Termination & Docs Update

- **Files**: install.py, desktop.py, locales/*.json (16 files), skills/post-write-check/SKILL.md, skills/i18n-audit/SKILL.md, AGENTS.md, docs/context.md
- **Purpose**: Automate ffmpeg installation across platforms, change desktop app close behavior, and ensure documentation integrity.
- **Decisions**:
  - `install.py`: Added automated installation using package managers (`winget`, `brew`, `apt`) with a fallback to the manual instruction print on failure.
  - Added `install.ffmpeg_installing`, `install.ffmpeg_success`, `install.ffmpeg_fail` keys to all 16 locale files via python script to pass the i18n parity audit.
  - `desktop.py`: Changed pywebview `closed` event to push `"quit"` to the queue instead of `"closed"`, making the app fully exit instead of hiding in the tray.
  - Docs: Replaced hardcoded root-level test paths (`test_i18n.py` and `test_app.py`) with `tests/` prefix in agent instructions and skills.
- **Issues**:
  - Global `GEMINI.md` check triggered `audit.ps1` warning about absolute paths, but it's an expected behavior for the Gemini absolute context loading rule.
