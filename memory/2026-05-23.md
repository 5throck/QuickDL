## Documentation Consistency Audit & Fixes
- **Files**: `agents/pm.md`, `agents/qa.md`, `agents/backend.md`, `agents/i18n.md`, `CLAUDE.md`, `scripts/sync-md.sh`, `scripts/sync-md.ps1`, `.claude/commands/changelog.md`, `.github/pull_request_template.md`, `skills/post-write-check/SKILL.md`, `CHANGELOG.md`
- **Purpose**: Perform a rigorous review and unification of all markdown files across the project to eliminate discrepancies in paths, test commands, and agent rules.
- **Decisions**: Aligned all test execution commands to `pytest tests/` (e.g. `test_app.py` → `tests/test_app.py`) to prevent "file not found" errors during agent executions. Added missing YAML frontmatter to `.claude/commands/changelog.md` to restore Claude Code command integrity. Standardized the GitHub PR template to use the project's canonical test command.
- **Issues**: None remaining. The SSoT (`docs/context.md`) and all adjacent AI configuration files are now perfectly synchronized.
